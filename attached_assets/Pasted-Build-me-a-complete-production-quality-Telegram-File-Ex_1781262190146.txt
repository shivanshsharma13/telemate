Build me a complete production-quality Telegram File Explorer web application that can run on Replit. Use whatever architecture, framework, libraries, database, frontend, and background processing approach you believe is best. The application must use Telethon for Telegram access, and I will provide my own Telegram api_id and api_hash. After starting the application, I should be able to authenticate once with Telegram and persist the session. The application should fetch all channels and groups accessible to my Telegram account and display them in a modern searchable UI. I should be able to search, sort, and filter channels, open any channel, and view all files inside it. Files should display filename, extension, size, upload date, and message ID. The UI must support Select All, Deselect All, multi-select, search within files, pagination for very large channels, and display total selected files and total selected size. When I click Download Selected, the application should download all selected Telegram files, package them into a ZIP archive, and provide a download link. It should also support Download Entire Channel. The implementation must be optimized for large channels containing thousands or tens of thousands of files. Files should be streamed directly to disk, never loaded entirely into RAM, and ZIP creation should be efficient and scalable. Add progress tracking, status updates, error handling, resume capability where practical, and automatic cleanup of temporary files. Design the UI to feel like Google Drive or Dropbox with a clean modern experience. Generate complete runnable code, folder structure, setup instructions, configuration files, environment variable handling, database schema if needed, all backend and frontend code, and any helper services required. Do not provide pseudocode or partial snippets. Make architectural decisions yourself and build the best possible version that runs successfully on Replit and can later be extended into a larger application if needed.
